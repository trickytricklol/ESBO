# Core dependencies (PyTorch >= 1.8, CUDA required)
torch>=1.8.0
torchvision>=0.9.0
numpy
h5py
scipy
scikit-learn
tqdm
matplotlib
einops
pyyaml
tensorboardX
pandas

# PointKAN (pointkan/) requires compiled extensions:
#   - pointnet2_ops (see upstream PointKAN-pytorch repo)
#   - rational_kat_cu (KAN rational kernel, see upstream repo)
